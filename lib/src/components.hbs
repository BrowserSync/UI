---
layout: components.hbs
title: "Components"
sections:
    - name: "Typography"
      file: "_components/type.hbs"
      tube: true
    - name: "Buttons"
      file: "_components/buttons.hbs"
      tube: true
    - name: "Headings"
      file: "_components/heading.hbs"
      tube: true
    - name: "Top Bar"
      file: "_components/top-bar.hbs"
      tube: false
---
{{ inc src="../img/icons/svg/symbols.svg" }}

<div class="tube">
    <h1>BrowserSync UI Components</h1>
    <hr/>
</div>

    {{#each page.sections}}
        {{#if this.tube}}
            <div class="tube">
        {{/if}}
        <h1>{{this.name}}</h1>
        {{inc src=this.file title=this.name }}
        {{#if this.tube}}
            </div>
        {{/if}}
        <hr/>
    {{/each}}
